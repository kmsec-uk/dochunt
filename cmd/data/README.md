# Data

This README is simply here to preserve the data directory. For my use-case
it is where I store the database for both the `/cmd/get` (commoncrawl) and
`/cmd/urlscan` (urlscan) clients. The db itself is not version tracked 
(it's large).